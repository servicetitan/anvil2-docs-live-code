import { HelperProps } from '../internal/components/Helper';
export type MaxLengthCounterProps = {
    showCounter: true;
    maxLength: HelperProps["maxLength"];
} | {
    showCounter?: false;
    maxLength?: HelperProps["maxLength"];
};
