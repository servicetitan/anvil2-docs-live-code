import { ComponentPropsWithoutRef, FC, ReactNode } from 'react';
import { LayoutUtilProps } from '../../types';
import { Token } from '@servicetitan/hammer-token';
export type CustomThemeType = {
    primitive?: Partial<Token["primitive"]>;
    semantic?: Partial<Token["semantic"]>;
    component?: Partial<Token["component"]>;
};
export type ThemeProviderProps = Omit<ComponentPropsWithoutRef<"div">, "prefix"> & LayoutUtilProps & {
    theme?: CustomThemeType;
    mode?: "light" | "dark";
    children?: ReactNode;
};
export declare const ThemeProvider: FC<ThemeProviderProps>;
