import { MaskitoDateMode } from '@maskito/kit';
export type DateMode = Extract<MaskitoDateMode, "dd/mm/yyyy" | "mm/dd/yyyy" | "yyyy/mm/dd">;
export type YearlessDateMode = Extract<MaskitoDateMode, "mm/dd" | "dd/mm">;
export type YearlessDate = {
    day: number | null;
    month: number | null;
};
