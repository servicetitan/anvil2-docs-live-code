import { ChangeEvent } from 'react';
import { IanaZone, Locale } from '../../types';
import { DateRangeSelect, DateSelect } from './DateField';
export interface DateFieldCommonProps {
    /** Date format for input and display */
    format?: "MM/dd/yyyy" | "dd/MM/yyyy";
    /** Locale for date formatting */
    locale?: Locale;
    /** Default timezone for date operations */
    defaultTimeZone?: IanaZone;
    /** Whether to open calendar on mount */
    openCalendar?: boolean;
    /** Whether to disable the format hint */
    disableHint?: boolean;
}
/**
 * Props for single date selection mode
 */
export interface DateFieldSingleProps {
    /** Callback when date selection changes */
    onChange?: (event: ChangeEvent<HTMLInputElement>, data: DateSelect) => void;
    /** Controlled value for the date field */
    value?: string;
    /** Default value for the date field */
    defaultValue?: string;
    /** Must be false for single date mode */
    range?: false;
}
/**
 * Props for date range selection mode
 */
export interface DateFieldRangeProps {
    /** Callback when date range selection changes */
    onChange?: (event: ChangeEvent<HTMLInputElement>, data: DateRangeSelect) => void;
    /** Controlled value for the date range field */
    value?: {
        start?: string;
        end?: string;
    };
    /** Default value for the date range field */
    defaultValue?: {
        start?: string;
        end?: string;
    };
    /** Must be true for range mode */
    range: true;
}
