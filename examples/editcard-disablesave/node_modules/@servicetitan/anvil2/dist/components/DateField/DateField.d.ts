import { DataTrackingId } from '../../types';
import { CalendarProps } from '../Calendar';
import { TextFieldProps } from '../TextField';
import { DateFieldCommonProps, DateFieldRangeProps, DateFieldSingleProps } from './types';
/**
 * Data structure for a single date selection
 */
export type DateSelect = {
    /** The formatted date string value */
    value: string;
    /** The ISO date string or null if invalid */
    date: string | null | undefined;
    /** Whether the date is valid according to constraints */
    isValid: boolean;
};
/**
 * Data structure for a date range selection
 */
export type DateRangeSelect = {
    /** The start date selection */
    start?: DateSelect;
    /** The end date selection */
    end?: DateSelect;
    /** The combined formatted range string */
    value: string;
};
/**
 * Props for the DateField component
 */
export type DateFieldProps = Omit<CalendarProps, "onChange" | "value" | "defaultValue"> & Omit<TextFieldProps, "prefix" | "suffix" | "hint" | "onChange" | "showCounter" | "value" | "defaultValue"> & DataTrackingId & DateFieldCommonProps & (DateFieldSingleProps | DateFieldRangeProps);
/**
 * DateField component for selecting dates with calendar popup and input validation
 * @deprecated Use DateFieldSingle or DateFieldRange instead
 */
export declare const DateField: import('react').ForwardRefExoticComponent<DateFieldProps & import('react').RefAttributes<HTMLInputElement>>;
