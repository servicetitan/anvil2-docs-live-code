import { ComponentPropsWithoutRef } from 'react';
import { DataTrackingId, LayoutUtilProps, OnCloseProps, Status } from '../../types';
/**
 * Props for the Alert component
 * @extends ComponentPropsWithoutRef<"div">
 * @extends LayoutUtilProps
 * @extends OnCloseProps
 */
export type AlertProps = ComponentPropsWithoutRef<"div"> & LayoutUtilProps & OnCloseProps & {
    /**
     * The visual style of an alert.
     * @default info
     */
    status?: Status;
    /**
     * The title text displayed in the alert.
     */
    title: string;
} & 
/**
 * Data tracking id
 */
DataTrackingId;
/**
 * Alert component for displaying important messages to users.
 *
 * Features:
 * - Supports different visual styles (info, success, warning, danger)
 * - Optional close button for dismissible alerts
 * - Customizable title and content
 * - Accessible with proper ARIA roles
 * - Includes status-specific icons
 * - Supports layout utilities for positioning and spacing
 * - Automatic tracking ID generation for analytics
 *
 * @example
 * <Alert
 *   title="System Update"
 *   onClose={() => console.log('Alert closed')}
 * >
 *   The system will be updated tonight.
 * </Alert>
 *
 * @example
 * <Alert
 *   status="success"
 *   title="Operation Complete"
 * >
 *   Your changes have been saved successfully.
 * </Alert>
 */
export declare const Alert: import('react').ForwardRefExoticComponent<Omit<import('react').DetailedHTMLProps<import('react').HTMLAttributes<HTMLDivElement>, HTMLDivElement>, "ref"> & LayoutUtilProps & OnCloseProps & {
    /**
     * The visual style of an alert.
     * @default info
     */
    status?: Status;
    /**
     * The title text displayed in the alert.
     */
    title: string;
} & DataTrackingId & import('react').RefAttributes<HTMLDivElement>>;
