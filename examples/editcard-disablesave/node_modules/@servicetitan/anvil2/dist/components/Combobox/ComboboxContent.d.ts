import { ComponentPropsWithoutRef, ReactNode, CSSProperties } from 'react';
import { UseComboboxProps as UseDownshiftComboboxProps, UseSelectProps as UseDownshiftSelectProps, UseComboboxReturnValue as UseDownshiftComboboxReturnValue, UseSelectReturnValue as UseDownshiftSelectReturnValue } from 'downshift';
/**
 * Props for the options passed to the children render function
 */
export type ComboboxContentOptionsProps<Item> = {
    /** Props for the option including selection and disabled state */
    optionProps: ReturnType<UseDownshiftComboboxReturnValue<Item>["getItemProps"]> & {
        selected: boolean;
        disabled: boolean;
    };
    /** The item to render */
    item: Item;
    /** The index of the item in the list */
    index: number;
};
/**
 * Props for the ComboboxContent component
 */
export type ComboboxContentProps<Item> = Omit<ComponentPropsWithoutRef<"div">, "children"> & {
    /** Render function that receives combobox state and returns content */
    children?: ({ items, inputValue, selectedItem, getMenuProps, getItemProps, }: Pick<UseDownshiftComboboxProps<Item> | UseDownshiftSelectProps<Item>, "items"> & Pick<UseDownshiftComboboxReturnValue<Item> | UseDownshiftSelectReturnValue<Item>, "getMenuProps" | "getItemProps" | "inputValue" | "selectedItem"> & {
        listProps: ReturnType<UseDownshiftComboboxReturnValue<Item>["getMenuProps"] | UseDownshiftSelectReturnValue<Item>["getMenuProps"]> & {
            style: CSSProperties;
        };
        options: ComboboxContentOptionsProps<Item>[];
    }) => ReactNode;
    /** Whether to disable popover behavior and render inline */
    disablePopover?: boolean;
};
/**
 * ComboboxContent component for rendering the content area of a combobox
 *
 * Features:
 * - Supports both popover and inline rendering modes
 * - Provides render props pattern for flexible content rendering
 * - Handles grouped items with proper ARIA labels
 * - Integrates with combobox state management
 * - Supports custom styling and layout
 * - Manages focus and blur events for popover behavior
 *
 * @template Item - The type of items in the combobox
 * @param props - The component props
 * @returns A rendered combobox content area
 *
 * @example
 * <Combobox.Content>
 *   {({ items }) => (
 *     <ul>
 *       {items.map((item, index) => (
 *         <li key={item.id}>
 *           {item.name}
 *         </li>
 *       ))}
 *     </ul>
 *   )}
 * </Combobox.Content>
 */
export declare const ComboboxContent: <Item = any>({ children, className, disablePopover, ...rest }: ComboboxContentProps<Item>) => import("react/jsx-runtime").JSX.Element;
