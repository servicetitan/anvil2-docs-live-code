import { FieldMessage } from "@servicetitan/anvil2";

function App() {
  return (
    <FieldMessage
      hint="Hint text"
      errorMessage="Field message in error state"
    />
  );
}

export default App;
